name: 'triggohib'
version: '1.0.0'

profile: 'triggohib'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  triggohib:
    staging:
      +materialized: view       # views rápidas para staging
    intermediate:
      +materialized: view       # views intermediárias, pode mudar para table se quiser
    marts:
      +materialized: table      # dimensões e fatos como tabelas para performance
